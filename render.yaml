services:
  - type: web
    name: cpm-bot
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python cpm_bot.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.4
